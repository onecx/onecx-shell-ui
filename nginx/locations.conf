location @@APP_BASE_HREFshell-bff/ {
  proxy_pass @@BFF_URL;
  proxy_set_header Host            $host;
  proxy_set_header X-Forwarded-For $remote_addr;
}

location ~* \.map$ {
  rewrite ^@@APP_BASE_HREF(.*)$ /$1 break;
  @@SOURCE_MAPS_ACCESS_RULE
}
